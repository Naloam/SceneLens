/**
 * Background 模块导出
 * 
 * 后台服务：负责在应用后台时继续进行场景检测
 */

export { 
  BackgroundService, 
  backgroundService,
  type BackgroundTaskStatus,
  type BackgroundServiceConfig,
  type DetectionResult,
} from './BackgroundService';
